services:
  - type: web
    name: horary-oracle-api
    runtime: python
    plan: free
    buildCommand: pip install -r horary_oracle/requirements.txt
    startCommand: uvicorn horary_oracle.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
      - key: PYTHON_VERSION
        value: "3.12.8"
# horary-oracle-web (streamlit) KALDIRILDI 2026-09-25: API kredisini tamamen atliyordu
# (horary_app.py dogrudan cast_horary_chart + call_openai cagiriyor, login/kredi yok)
# => herkes login'siz sınırsız soru sorabiliyordu. Servis Render'da durdurulmali/silinmeli.

